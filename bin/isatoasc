#!/usr/bin/env fish

for file in $argv
    sd -F '𝟬' '\<zero>' $file
    sd -F '𝟭' '\<one>' $file
    sd -F '𝟮' '\<two>' $file
    sd -F '𝟯' '\<three>' $file
    sd -F '𝟰' '\<four>' $file
    sd -F '𝟱' '\<five>' $file
    sd -F '𝟲' '\<six>' $file
    sd -F '𝟳' '\<seven>' $file
    sd -F '𝟴' '\<eight>' $file
    sd -F '𝟵' '\<nine>' $file
    sd -F '𝒜' '\<A>' $file
    sd -F 'ℬ' '\<B>' $file
    sd -F '𝒞' '\<C>' $file
    sd -F '𝒟' '\<D>' $file
    sd -F 'ℰ' '\<E>' $file
    sd -F 'ℱ' '\<F>' $file
    sd -F '𝒢' '\<G>' $file
    sd -F 'ℋ' '\<H>' $file
    sd -F 'ℐ' '\<I>' $file
    sd -F '𝒥' '\<J>' $file
    sd -F '𝒦' '\<K>' $file
    sd -F 'ℒ' '\<L>' $file
    sd -F 'ℳ' '\<M>' $file
    sd -F '𝒩' '\<N>' $file
    sd -F '𝒪' '\<O>' $file
    sd -F '𝒫' '\<P>' $file
    sd -F '𝒬' '\<Q>' $file
    sd -F 'ℛ' '\<R>' $file
    sd -F '𝒮' '\<S>' $file
    sd -F '𝒯' '\<T>' $file
    sd -F '𝒰' '\<U>' $file
    sd -F '𝒱' '\<V>' $file
    sd -F '𝒲' '\<W>' $file
    sd -F '𝒳' '\<X>' $file
    sd -F '𝒴' '\<Y>' $file
    sd -F '𝒵' '\<Z>' $file
    sd -F '𝖺' '\<a>' $file
    sd -F '𝖻' '\<b>' $file
    sd -F '𝖼' '\<c>' $file
    sd -F '𝖽' '\<d>' $file
    sd -F '𝖾' '\<e>' $file
    sd -F '𝖿' '\<f>' $file
    sd -F '𝗀' '\<g>' $file
    sd -F '𝗁' '\<h>' $file
    sd -F '𝗂' '\<i>' $file
    sd -F '𝗃' '\<j>' $file
    sd -F '𝗄' '\<k>' $file
    sd -F '𝗅' '\<l>' $file
    sd -F '𝗆' '\<m>' $file
    sd -F '𝗇' '\<n>' $file
    sd -F '𝗈' '\<o>' $file
    sd -F '𝗉' '\<p>' $file
    sd -F '𝗊' '\<q>' $file
    sd -F '𝗋' '\<r>' $file
    sd -F '𝗌' '\<s>' $file
    sd -F '𝗍' '\<t>' $file
    sd -F '𝗎' '\<u>' $file
    sd -F '𝗏' '\<v>' $file
    sd -F '𝗐' '\<w>' $file
    sd -F '𝗑' '\<x>' $file
    sd -F '𝗒' '\<y>' $file
    sd -F '𝗓' '\<z>' $file
    sd -F '𝔄' '\<AA>' $file
    sd -F '𝔅' '\<BB>' $file
    sd -F 'ℭ' '\<CC>' $file
    sd -F '𝔇' '\<DD>' $file
    sd -F '𝔈' '\<EE>' $file
    sd -F '𝔉' '\<FF>' $file
    sd -F '𝔊' '\<GG>' $file
    sd -F 'ℌ' '\<HH>' $file
    sd -F 'ℑ' '\<II>' $file
    sd -F '𝔍' '\<JJ>' $file
    sd -F '𝔎' '\<KK>' $file
    sd -F '𝔏' '\<LL>' $file
    sd -F '𝔐' '\<MM>' $file
    sd -F '𝔑' '\<NN>' $file
    sd -F '𝔒' '\<OO>' $file
    sd -F '𝔓' '\<PP>' $file
    sd -F '𝔔' '\<QQ>' $file
    sd -F 'ℜ' '\<RR>' $file
    sd -F '𝔖' '\<SS>' $file
    sd -F '𝔗' '\<TT>' $file
    sd -F '𝔘' '\<UU>' $file
    sd -F '𝔙' '\<VV>' $file
    sd -F '𝔚' '\<WW>' $file
    sd -F '𝔛' '\<XX>' $file
    sd -F '𝔜' '\<YY>' $file
    sd -F 'ℨ' '\<ZZ>' $file
    sd -F '𝔞' '\<aa>' $file
    sd -F '𝔟' '\<bb>' $file
    sd -F '𝔠' '\<cc>' $file
    sd -F '𝔡' '\<dd>' $file
    sd -F '𝔢' '\<ee>' $file
    sd -F '𝔣' '\<ff>' $file
    sd -F '𝔤' '\<gg>' $file
    sd -F '𝔥' '\<hh>' $file
    sd -F '𝔦' '\<ii>' $file
    sd -F '𝔧' '\<jj>' $file
    sd -F '𝔨' '\<kk>' $file
    sd -F '𝔩' '\<ll>' $file
    sd -F '𝔪' '\<mm>' $file
    sd -F '𝔫' '\<nn>' $file
    sd -F '𝔬' '\<oo>' $file
    sd -F '𝔭' '\<pp>' $file
    sd -F '𝔮' '\<qq>' $file
    sd -F '𝔯' '\<rr>' $file
    sd -F '𝔰' '\<ss>' $file
    sd -F '𝔱' '\<tt>' $file
    sd -F '𝔲' '\<uu>' $file
    sd -F '𝔳' '\<vv>' $file
    sd -F '𝔴' '\<ww>' $file
    sd -F '𝔵' '\<xx>' $file
    sd -F '𝔶' '\<yy>' $file
    sd -F '𝔷' '\<zz>' $file
    sd -F 'α' '\<alpha>' $file
    sd -F 'β' '\<beta>' $file
    sd -F 'γ' '\<gamma>' $file
    sd -F 'δ' '\<delta>' $file
    sd -F 'ε' '\<epsilon>' $file
    sd -F 'ζ' '\<zeta>' $file
    sd -F 'η' '\<eta>' $file
    sd -F 'θ' '\<theta>' $file
    sd -F 'ι' '\<iota>' $file
    sd -F 'κ' '\<kappa>' $file
    sd -F 'λ' '\<lambda>' $file
    sd -F 'μ' '\<mu>' $file
    sd -F 'ν' '\<nu>' $file
    sd -F 'ξ' '\<xi>' $file
    sd -F 'π' '\<pi>' $file
    sd -F 'ρ' '\<rho>' $file
    sd -F 'σ' '\<sigma>' $file
    sd -F 'τ' '\<tau>' $file
    sd -F 'υ' '\<upsilon>' $file
    sd -F 'φ' '\<phi>' $file
    sd -F 'χ' '\<chi>' $file
    sd -F 'ψ' '\<psi>' $file
    sd -F 'ω' '\<omega>' $file
    sd -F 'Γ' '\<Gamma>' $file
    sd -F 'Δ' '\<Delta>' $file
    sd -F 'Θ' '\<Theta>' $file
    sd -F 'Λ' '\<Lambda>' $file
    sd -F 'Ξ' '\<Xi>' $file
    sd -F 'Π' '\<Pi>' $file
    sd -F 'Σ' '\<Sigma>' $file
    sd -F 'Υ' '\<Upsilon>' $file
    sd -F 'Φ' '\<Phi>' $file
    sd -F 'Ψ' '\<Psi>' $file
    sd -F 'Ω' '\<Omega>' $file
    sd -F '𝔸' '\<bbbA>' $file
    sd -F '𝔹' '\<bool>' $file
    sd -F 'ℂ' '\<complex>' $file
    sd -F '𝔻' '\<bbbD>' $file
    sd -F '𝔼' '\<bbbE>' $file
    sd -F '𝔽' '\<bbbF>' $file
    sd -F '𝔾' '\<bbbG>' $file
    sd -F 'ℍ' '\<bbbH>' $file
    sd -F '𝕀' '\<bbbI>' $file
    sd -F '𝕁' '\<bbbJ>' $file
    sd -F '𝕂' '\<bbbK>' $file
    sd -F '𝕃' '\<bbbL>' $file
    sd -F '𝕄' '\<bbbM>' $file
    sd -F 'ℕ' '\<nat>' $file
    sd -F '𝕆' '\<bbbO>' $file
    sd -F 'ℙ' '\<bbbP>' $file
    sd -F 'ℚ' '\<rat>' $file
    sd -F 'ℝ' '\<real>' $file
    sd -F '𝕊' '\<bbbS>' $file
    sd -F '𝕋' '\<bbbT>' $file
    sd -F '𝕌' '\<bbbU>' $file
    sd -F '𝕍' '\<bbbV>' $file
    sd -F '𝕎' '\<bbbW>' $file
    sd -F '𝕏' '\<bbbX>' $file
    sd -F '𝕐' '\<bbbY>' $file
    sd -F 'ℤ' '\<int>' $file
    sd -F '←' '\<leftarrow>' $file
    sd -F '⟵' '\<longleftarrow>' $file
    sd -F '⤎' '\<longlongleftarrow>' $file
    sd -F '⇠' '\<longlonglongleftarrow>' $file
    sd -F '→' '\<rightarrow>' $file
    sd -F '⟶' '\<longrightarrow>' $file
    sd -F '⤏' '\<longlongrightarrow>' $file
    sd -F '⇢' '\<longlonglongrightarrow>' $file
    sd -F '⇐' '\<Leftarrow>' $file
    sd -F '⟸' '\<Longleftarrow>' $file
    sd -F '⇚' '\<Lleftarrow>' $file
    sd -F '⇒' '\<Rightarrow>' $file
    sd -F '⟹' '\<Longrightarrow>' $file
    sd -F '⇛' '\<Rrightarrow>' $file
    sd -F '↔' '\<leftrightarrow>' $file
    sd -F '⟷' '\<longleftrightarrow>' $file
    sd -F '⇔' '\<Leftrightarrow>' $file
    sd -F '⟺' '\<Longleftrightarrow>' $file
    sd -F '↦' '\<mapsto>' $file
    sd -F '⟼' '\<longmapsto>' $file
    sd -F '─' '\<midarrow>' $file
    sd -F '═' '\<Midarrow>' $file
    sd -F '↩' '\<hookleftarrow>' $file
    sd -F '↪' '\<hookrightarrow>' $file
    sd -F '↽' '\<leftharpoondown>' $file
    sd -F '⇁' '\<rightharpoondown>' $file
    sd -F '↼' '\<leftharpoonup>' $file
    sd -F '⇀' '\<rightharpoonup>' $file
    sd -F '⇌' '\<rightleftharpoons>' $file
    sd -F '↝' '\<leadsto>' $file
    sd -F '⇃' '\<downharpoonleft>' $file
    sd -F '⇂' '\<downharpoonright>' $file
    sd -F '↿' '\<upharpoonleft>' $file
    sd -F '↾' '\<restriction>' $file
    sd -F '∷' '\<Colon>' $file
    sd -F '↑' '\<up>' $file
    sd -F '⇑' '\<Up>' $file
    sd -F '↓' '\<down>' $file
    sd -F '⇓' '\<Down>' $file
    sd -F '↕' '\<updown>' $file
    sd -F '⇕' '\<Updown>' $file
    sd -F '⟨' '\<langle>' $file
    sd -F '⟩' '\<rangle>' $file
    sd -F '⟪' '\<llangle>' $file
    sd -F '⟫' '\<rrangle>' $file
    sd -F '⌈' '\<lceil>' $file
    sd -F '⌉' '\<rceil>' $file
    sd -F '⌊' '\<lfloor>' $file
    sd -F '⌋' '\<rfloor>' $file
    sd -F '⦇' '\<lparr>' $file
    sd -F '⦈' '\<rparr>' $file
    sd -F '⟦' '\<lbrakk>' $file
    sd -F '⟧' '\<rbrakk>' $file
    sd -F '⦃' '\<lbrace>' $file
    sd -F '⦄' '\<rbrace>' $file
    sd -F '⦉' '\<lblot>' $file
    sd -F '⦊' '\<rblot>' $file
    sd -F '«' '\<guillemotleft>' $file
    sd -F '»' '\<guillemotright>' $file
    sd -F '⊥' '\<bottom>' $file
    sd -F '⊤' '\<top>' $file
    sd -F '∧' '\<and>' $file
    sd -F '⋀' '\<And>' $file
    sd -F '∨' '\<or>' $file
    sd -F '⋁' '\<Or>' $file
    sd -F '∀' '\<forall>' $file
    sd -F '∃' '\<exists>' $file
    sd -F '∄' '\<nexists>' $file
    sd -F '¬' '\<not>' $file
    sd -F '○' '\<circle>' $file
    sd -F '□' '\<box>' $file
    sd -F '◇' '\<diamond>' $file
    sd -F '⋄' '\<diamondop>' $file
    sd -F '⊢' '\<turnstile>' $file
    sd -F '⊨' '\<Turnstile>' $file
    sd -F '⊩' '\<tturnstile>' $file
    sd -F '⊫' '\<TTurnstile>' $file
    sd -F '⊣' '\<stileturn>' $file
    sd -F '√' '\<surd>' $file
    sd -F '≤' '\<le>' $file
    sd -F '≥' '\<ge>' $file
    sd -F '≪' '\<lless>' $file
    sd -F '≫' '\<ggreater>' $file
    sd -F '≲' '\<lesssim>' $file
    sd -F '≳' '\<greatersim>' $file
    sd -F '⪅' '\<lessapprox>' $file
    sd -F '⪆' '\<greaterapprox>' $file
    sd -F '∈' '\<in>' $file
    sd -F '∉' '\<notin>' $file
    sd -F '⊂' '\<subset>' $file
    sd -F '⊃' '\<supset>' $file
    sd -F '⊆' '\<subseteq>' $file
    sd -F '⊇' '\<supseteq>' $file
    sd -F '⊏' '\<sqsubset>' $file
    sd -F '⊐' '\<sqsupset>' $file
    sd -F '⊑' '\<sqsubseteq>' $file
    sd -F '⊒' '\<sqsupseteq>' $file
    sd -F '∩' '\<inter>' $file
    sd -F '⋂' '\<Inter>' $file
    sd -F '∪' '\<union>' $file
    sd -F '⋃' '\<Union>' $file
    sd -F '⊔' '\<squnion>' $file
    sd -F '⨆' '\<Squnion>' $file
    sd -F '⊓' '\<sqinter>' $file
    sd -F '⨅' '\<Sqinter>' $file
    sd -F '∖' '\<setminus>' $file
    sd -F '∝' '\<propto>' $file
    sd -F '⊎' '\<uplus>' $file
    sd -F '⨄' '\<Uplus>' $file
    sd -F '≠' '\<noteq>' $file
    sd -F '∼' '\<sim>' $file
    sd -F '≐' '\<doteq>' $file
    sd -F '≃' '\<simeq>' $file
    sd -F '≈' '\<approx>' $file
    sd -F '≍' '\<asymp>' $file
    sd -F '≅' '\<cong>' $file
    sd -F '⌣' '\<smile>' $file
    sd -F '≡' '\<equiv>' $file
    sd -F '⌢' '\<frown>' $file
    sd -F '⋈' '\<Join>' $file
    sd -F '⨝' '\<bowtie>' $file
    sd -F '≺' '\<prec>' $file
    sd -F '≻' '\<succ>' $file
    sd -F '≼' '\<preceq>' $file
    sd -F '≽' '\<succeq>' $file
    sd -F '∥' '\<parallel>' $file
    sd -F '‖' '\<Parallel>' $file
    sd -F '⫴' '\<interleave>' $file
    sd -F '⫽' '\<sslash>' $file
    sd -F '¦' '\<bar>' $file
    sd -F '⫿' '\<bbar>' $file
    sd -F '±' '\<plusminus>' $file
    sd -F '∓' '\<minusplus>' $file
    sd -F '×' '\<times>' $file
    sd -F '÷' '\<div>' $file
    sd -F '⋅' '\<cdot>' $file
    sd -F '⋆' '\<star>' $file
    sd -F '∙' '\<bullet>' $file
    sd -F '∘' '\<circ>' $file
    sd -F '†' '\<dagger>' $file
    sd -F '‡' '\<ddagger>' $file
    sd -F '⊲' '\<lhd>' $file
    sd -F '⊳' '\<rhd>' $file
    sd -F '⊴' '\<unlhd>' $file
    sd -F '⊵' '\<unrhd>' $file
    sd -F '◃' '\<triangleleft>' $file
    sd -F '▹' '\<triangleright>' $file
    sd -F '△' '\<triangle>' $file
    sd -F '≜' '\<triangleq>' $file
    sd -F '⊕' '\<oplus>' $file
    sd -F '⨁' '\<Oplus>' $file
    sd -F '⊗' '\<otimes>' $file
    sd -F '⨂' '\<Otimes>' $file
    sd -F '⊙' '\<odot>' $file
    sd -F '⨀' '\<Odot>' $file
    sd -F '⊖' '\<ominus>' $file
    sd -F '⊘' '\<oslash>' $file
    sd -F '…' '\<dots>' $file
    sd -F '⋯' '\<cdots>' $file
    sd -F '∑' '\<Sum>' $file
    sd -F '∏' '\<Prod>' $file
    sd -F '∐' '\<Coprod>' $file
    sd -F '∞' '\<infinity>' $file
    sd -F '∫' '\<integral>' $file
    sd -F '∮' '\<ointegral>' $file
    sd -F '♣' '\<clubsuit>' $file
    sd -F '♢' '\<diamondsuit>' $file
    sd -F '♡' '\<heartsuit>' $file
    sd -F '♠' '\<spadesuit>' $file
    sd -F 'ℵ' '\<aleph>' $file
    sd -F '∅' '\<emptyset>' $file
    sd -F '∇' '\<nabla>' $file
    sd -F '∂' '\<partial>' $file
    sd -F '♭' '\<flat>' $file
    sd -F '♮' '\<natural>' $file
    sd -F '♯' '\<sharp>' $file
    sd -F '∠' '\<angle>' $file
    sd -F '©' '\<copyright>' $file
    sd -F '®' '\<registered>' $file
    sd -F '‐' '\<hyphen>' $file
    sd -F '¯' '\<inverse>' $file
    sd -F '·' '\<sqdot>' $file
    sd -F '¼' '\<onequarter>' $file
    sd -F '½' '\<onehalf>' $file
    sd -F '¾' '\<threequarters>' $file
    sd -F 'ª' '\<ordfeminine>' $file
    sd -F 'º' '\<ordmasculine>' $file
    sd -F '§' '\<section>' $file
    sd -F '¶' '\<paragraph>' $file
    sd -F '¡' '\<exclamdown>' $file
    sd -F '¿' '\<questiondown>' $file
    sd -F '€' '\<euro>' $file
    sd -F '£' '\<pounds>' $file
    sd -F '¥' '\<yen>' $file
    sd -F '¢' '\<cent>' $file
    sd -F '¤' '\<currency>' $file
    sd -F '°' '\<degree>' $file
    sd -F '⨿' '\<amalg>' $file
    sd -F '℧' '\<mho>' $file
    sd -F '◊' '\<lozenge>' $file
    sd -F '℘' '\<wp>' $file
    sd -F '≀' '\<wrong>' $file
    sd -F '´' '\<acute>' $file
    sd -F 'ı' '\<index>' $file
    sd -F '¨' '\<dieresis>' $file
    sd -F '¸' '\<cedilla>' $file
    sd -F '˝' '\<hungarumlaut>' $file
    sd -F '⤜' '\<bind>' $file
    sd -F '⪢' '\<then>' $file
    sd -F 'ϵ' '\<some>' $file
    sd -F '⨾' '\<Zcomp>' $file
    sd -F '↣' '\<Zinj>' $file
    sd -F '⤔' '\<Zpinj>' $file
    sd -F '⤕' '\<Zfinj>' $file
    sd -F '↠' '\<Zsurj>' $file
    sd -F '⤀' '\<Zpsurj>' $file
    sd -F '⤖' '\<Zbij>' $file
    sd -F '⇸' '\<Zpfun>' $file
    sd -F '⇻' '\<Zffun>' $file
    sd -F '◁' '\<Zdres>' $file
    sd -F '⩤' '\<Zndres>' $file
    sd -F '▷' '\<Zrres>' $file
    sd -F '⩥' '\<Znrres>' $file
    sd -F '⦁' '\<Zspot>' $file
    sd -F '⨟' '\<Zsemi>' $file
    sd -F '⨡' '\<Zproject>' $file
    sd -F '⦂' '\<Ztypecolon>' $file
    sd -F '⧹' '\<Zhide>' $file
    sd -F '⁀' '\<Zcat>' $file
    sd -F '⋿' '\<Zinbag>' $file
    sd -F '′' '\<Zprime>' $file
    sd -F '⌑' '\<hole>' $file
    sd -F '⏎' '\<newline>' $file
    sd -F '―' '\<comment>' $file
    sd -F '⌦' '\<cancel>' $file
    sd -F '✐' '\<marker>' $file
    sd -F '✓' '\<checkmark>' $file
    sd -F '✗' '\<crossmark>' $file
    sd -F '‹' '\<open>' $file
    sd -F '›' '\<close>' $file
    sd -F '⌂' '\<here>' $file
    sd -F '❖' '\<undefined>' $file
    sd -F '⇤' '\<noindent>' $file
    sd -F '┈' '\<smallskip>' $file
    sd -F '┉' '\<medskip>' $file
    sd -F '━' '\<bigskip>' $file
    sd -F '▪' '\<item>' $file
    sd -F '▸' '\<enum>' $file
    sd -F '➧' '\<descr>' $file
    sd -F '⁋' '\<footnote>' $file
    sd -F '▩' '\<verbatim>' $file
    sd -F '⬚' '\<theory_text>' $file
    sd -F '∗' '\<emph>' $file
    sd -F '❙' '\<bold>' $file
    sd -F '⇩' '\<sub>' $file
    sd -F '⇧' '\<sup>' $file
    sd -F '⇘' '\<bsub>' $file
    sd -F '⇙' '\<esub>' $file
    sd -F '⇗' '\<bsup>' $file
    sd -F '⇖' '\<esup>' $file
    sd -F '🗏' '\<file>' $file
    sd -F '🗀' '\<dir>' $file
    sd -F '🌐' '\<url>' $file
    sd -F '📓' '\<doc>' $file
    sd -F '☛' '\<action>' $file
end
